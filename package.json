{
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend run build",
    "start": "npm --prefix backend run migrate && npm --prefix backend run seed && npm --prefix backend start"
  }
}
